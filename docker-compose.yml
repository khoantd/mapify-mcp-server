version: '3.8'

services:
  mapify-mcp-server:
    build: .
    container_name: mapify-mcp-server
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - MAPIFY_API_KEY=${MAPIFY_API_KEY}
    volumes:
      - ./logs:/app/logs
    networks:
      - mapify-network

networks:
  mapify-network:
    driver: bridge
